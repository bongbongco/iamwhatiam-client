import React from "react";


// export default ({ id, name, subtitle, price, photoUrl }) => (
const TopicCardPresenter: React.SFC<any> = ({

}) => (
    <span>test</span>
);

export default TopicCardPresenter;